# Build output
bin/
obj/

# Visual Studio / Rider
.vs/
.idea/
*.user
*.suo

# NuGet
*.nupkg
packages/

# Do NOT ignore the strong-name key — it must be consistent across builds
# so the assembly's public key token stays stable in Dataverse.
!*.snk
